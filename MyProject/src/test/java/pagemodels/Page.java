/*package pagemodels;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Page {

    protected final WebDriver webDriver;
    protected final WebDriverWait webDriverWait;
    protected final PageContext pageContext;
    protected final String relativeUrl;
    protected final WebDriverWait webDriverNoWait;

    public Page(PageContext pageContext, String relativeUrl) {
        this.pageContext = pageContext;
        this.relativeUrl = relativeUrl;
        this.webDriver = pageContext.getWebDriver();
        this.webDriverWait = pageContext.getWebDriverWait();
        this.webDriverNoWait = new WebDriverWait(webDriver, 0);
    }
} */
